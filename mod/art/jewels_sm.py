# ФИГУРЫ ЛОКАЦИИ «СЕЙЛОР МУН»: шесть знаков планет 16x16.
#
# ЧИТАЕМОСТЬ — ГЛАВНОЕ. У камня ТРИ независимых признака, чтобы его можно было
# опознать краем глаза на скорости:
#   1) ФОРМА силуэта — у каждой планеты своя (как было в оригинале игры);
#   2) ЦВЕТ — шесть разнесённых оттенков, различимых и по тону, и по светлоте;
#   3) ЗНАК планеты белым поверх.
# Первая версия имела только цвет и знак (силуэт у всех был один) — и владелец
# сказал, что различать тяжело. Форма вернулась.
import math

# ---- палитра 2 (значение = (B<<9)|(G<<5)|(R<<1), каждый 0..7) ----
def rgb(r, g, b):
    return (b << 9) | (g << 5) | (r << 1)


PALETTE2 = [
    rgb(0, 0, 0),      # 0  прозрачный
    rgb(7, 7, 7),      # 1  белый — знак
    rgb(1, 0, 1),      # 2  тёмная обводка
    rgb(7, 7, 0),      # 3  Луна: чистый жёлтый
    rgb(5, 3, 0),      # 4  Луна: тень
    rgb(7, 0, 0),      # 5  Марс: чистый красный
    rgb(4, 0, 0),      # 6  Марс: тень
    rgb(7, 4, 6),      # 7  Венера: светлый розовый
    rgb(5, 1, 4),      # 8  Венера: тень
    rgb(0, 6, 7),      # 9  Меркурий: голубой
    rgb(0, 3, 5),      # 10 Меркурий: тень
    rgb(0, 7, 1),      # 11 Юпитер: зелёный
    rgb(0, 4, 0),      # 12 Юпитер: тень
    rgb(4, 1, 7),      # 13 Сатурн: глубокий фиолетовый
    rgb(2, 0, 4),      # 14 Сатурн: тень
    rgb(7, 7, 5),      # 15 общий блик
]

TONES = {
    'luna':     (3, 4),
    'mars':     (5, 6),
    'venera':   (7, 8),
    'merkuriy': (9, 10),
    'yupiter':  (11, 12),
    'saturn':   (13, 14),
}

N = 16
C = 7.5          # центр в координатах точек


# ---------------- силуэты: у каждой планеты свой ----------------
def _circle(x, y, r=7.6):
    return math.hypot(x - C, y - C) <= r


# Размеры подобраны так, чтобы в КАЖДЫЙ силуэт влезал общий знак (10x10 по
# центру): иначе силуэт срезает знаку хвосты и он перестаёт читаться.
def _square(x, y, s=6.6):
    dx, dy = abs(x - C), abs(y - C)
    if dx <= s - 2 or dy <= s - 2:
        return dx <= s and dy <= s
    return math.hypot(dx - (s - 2), dy - (s - 2)) <= 2      # скруглённые углы


def _diamond(x, y, r=9.4):
    return abs(x - C) + abs(y - C) <= r


def _octagon(x, y, s=7.4, r=11.0):
    dx, dy = abs(x - C), abs(y - C)
    return dx <= s and dy <= s and dx + dy <= r


def _hex_wide(x, y):
    dx, dy = abs(x - C), abs(y - C)
    return dy <= 6.2 and dx + dy * 0.75 <= 9.2


def _hex_tall(x, y):
    dx, dy = abs(x - C), abs(y - C)
    return dx <= 6.2 and dy + dx * 0.75 <= 9.2


SHAPES = {
    'luna':     _circle,      # круг
    'mars':     _square,      # квадрат
    'venera':   _diamond,     # ромб
    'merkuriy': _octagon,     # восьмиугольник
    'yupiter':  _hex_wide,    # шестиугольник лёжа
    'saturn':   _hex_tall,    # шестиугольник стоя
}

# ---------------- знаки планет (белым поверх силуэта) ----------------
# Все знаки вписаны в общий квадрат 10x10 по центру (ряды 3..12, колонки 3..12) —
# только так они не срезаются ни одним из шести силуэтов.
LUNA = [
    "................",
    "................",
    "................",
    ".........WWW....",
    ".......WWWWW....",
    "......WWWW......",
    ".....WWW........",
    ".....WWW........",
    ".....WWW........",
    ".....WWW........",
    "......WWWW......",
    ".......WWWWW....",
    ".........WWW....",
    "................",
    "................",
    "................",
]

MARS = [
    "................",
    "................",
    "................",
    ".........WWWW...",
    "..........WWW...",
    ".........W.WW...",
    "........W..W....",
    "...WWW.W........",
    "..W...WW........",
    "..W....W........",
    "..W...W.........",
    "...WWW..........",
    "................",
    "................",
    "................",
    "................",
]

VENERA = [
    "................",
    "................",
    "................",
    "......WWWW......",
    ".....W....W.....",
    ".....W....W.....",
    "......WWWW......",
    ".......WW.......",
    ".....WWWWWW.....",
    ".......WW.......",
    ".......WW.......",
    "................",
    "................",
    "................",
    "................",
    "................",
]

MERKURIY = [
    "................",
    "................",
    "................",
    "....W......W....",
    ".....W....W.....",
    "......WWWW......",
    ".....W....W.....",
    ".....W....W.....",
    "......WWWW......",
    ".......WW.......",
    ".....WWWWWW.....",
    ".......WW.......",
    "................",
    "................",
    "................",
    "................",
]

YUPITER = [
    "................",
    "................",
    "................",
    "................",
    "......WW........",
    ".....W..W.......",
    "....W....W......",
    ".........W......",
    "....WWWWWWW.....",
    ".........W......",
    ".........W......",
    ".........WWW....",
    "................",
    "................",
    "................",
    "................",
]

SATURN = [
    "................",
    "................",
    "................",
    "................",
    "....WW..........",
    ".....W..........",
    "...WWWWW........",
    ".....W..........",
    ".....W..WWW.....",
    ".....WWW...W....",
    "...........W....",
    "........WWWW....",
    "................",
    "................",
    "................",
    "................",
]

JEWELS = [
    ('luna', LUNA),
    ('mars', MARS),
    ('venera', VENERA),
    ('merkuriy', MERKURIY),
    ('yupiter', YUPITER),
    ('saturn', SATURN),
]


def build(name, sign):
    """Собирает 16x16 картинку индексов палитры: силуэт, обводка, тень, знак."""
    bright, dark = TONES[name]
    inside = SHAPES[name]
    mask = [[inside(x, y) for x in range(N)] for y in range(N)]

    out = [[0] * N for _ in range(N)]
    for y in range(N):
        for x in range(N):
            if not mask[y][x]:
                continue
            # край силуэта — обводка, чтобы камни не сливались друг с другом
            edge = any(not (0 <= x + dx < N and 0 <= y + dy < N and mask[y + dy][x + dx])
                       for dx, dy in ((1, 0), (-1, 0), (0, 1), (0, -1)))
            if edge:
                out[y][x] = 2
            else:
                # объём: тёмный только УГОЛ снизу-справа, а не половина камня —
                # иначе тень съедает силуэт и цвет читается хуже
                out[y][x] = dark if (x - C) + (y - C) > 6.5 else bright

    for y in range(N):
        for x in range(N):
            if sign[y][x] == 'W' and mask[y][x]:
                out[y][x] = 1

    # блик — только если знак туда не заходит
    if all(sign[y][x] != 'W' for y in range(2, 6) for x in range(2, 6)):
        for (y, x) in ((4, 4), (4, 5), (5, 4)):
            if mask[y][x] and out[y][x] != 2:
                out[y][x] = 15
    return out


def all_jewels():
    return [(n, build(n, s)) for n, s in JEWELS]


for n, s in JEWELS:
    assert len(s) == N, f"{n}: не {N} строк"
    for row in s:
        assert len(row) == N, f"{n}: строка не {N}: {row!r}"

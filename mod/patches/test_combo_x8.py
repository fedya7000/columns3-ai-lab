# ПРОБНАЯ правка — только чтобы доказать, что цепочка «прочитал -> изменил ->
# пересобрал -> запустилось» работает. В игру её не берём.
#
# Что меняем: бонус за длину цепочки в ядре начисления ($00DF7A)
#   было:  asl.w #$2, d2   ; цепочка * 4
#   стало: asl.w #$3, d2   ; цепочка * 8
PATCHES = [
    (0x00DF7A, bytes.fromhex('E742'), 'бонус за цепочку: *4 -> *8'),
]
